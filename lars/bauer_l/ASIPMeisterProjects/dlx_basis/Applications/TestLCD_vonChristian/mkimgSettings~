STACK_START="0x3BFC"

ADD_NOPS=4  # smaller than 1 means: don't call the corresponding script at all

MAX_ALLOWED_SUCCESSIVE_NOPS=4  # smaller than 0 means: don't call the corresponding script at all

